interface:
  display_name: "CodexDock Changelog"
  short_description: "Write codexdock changelog entries"
  default_prompt: "Use $codexdock-changelog to update CHANGELOG.md for this repository from the previous release tag to the current commit."
policy:
  allow_implicit_invocation: true
